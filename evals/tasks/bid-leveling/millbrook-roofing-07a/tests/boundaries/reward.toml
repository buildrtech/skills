# Local bucket = hard gates (never awards, never invents an amount): all-pass,
# weight 3. The disclaimers/ subdirectory is a weighted mean at weight 1.
weight = 3.0

[scoring]
aggregation = "all_pass"
